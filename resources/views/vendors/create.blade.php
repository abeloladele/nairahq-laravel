@extends('layouts.app')@section('content')<div class="card"><h1>Create Vendors</h1><form class="form" method="POST" action="{{ route('vendors.store') }}">@csrf<label>Name<input name="name" value="{{ old('name',$item->name ?? '') }}"></label><label>Email<input name="email" value="{{ old('email',$item->email ?? '') }}"></label><label>Phone<input name="phone" value="{{ old('phone',$item->phone ?? '') }}"></label><label>City<input name="city" value="{{ old('city',$item->city ?? '') }}"></label><label>State<input name="state" value="{{ old('state',$item->state ?? '') }}"></label><label>Country<input name="country" value="{{ old('country',$item->country ?? '') }}"></label>@foreach($errors->all() as $error)<div class="error">{{ $error }}</div>@endforeach<button class="btn">Save</button></form></div>@endsection