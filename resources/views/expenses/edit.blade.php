@extends('layouts.app')@section('content')<div class="card"><h1>Edit Expenses</h1><form class="form" method="POST" action="{{ route('expenses.update',$item) }}">@csrf @method('PUT')<label>Expense Date<input name="expense_date" value="{{ old('expense_date',$item->expense_date ?? '') }}"></label><label>Category<input name="category" value="{{ old('category',$item->category ?? '') }}"></label><label>Description<input name="description" value="{{ old('description',$item->description ?? '') }}"></label><label>Amount<input name="amount" value="{{ old('amount',$item->amount ?? '') }}"></label><label>Tax Amount<input name="tax_amount" value="{{ old('tax_amount',$item->tax_amount ?? '') }}"></label><label>Status<input name="status" value="{{ old('status',$item->status ?? '') }}"></label>@foreach($errors->all() as $error)<div class="error">{{ $error }}</div>@endforeach<button class="btn">Save</button></form></div>@endsection