@extends('layouts.app')@section('content')<div class="card"><h1>{{ $invoice->invoice_number }}</h1><p>{{ $invoice->customer->name }} <span class="badge">{{ $invoice->status }}</span></p><table class="table"><thead><tr><th>Description</th><th>Qty</th><th>Unit</th><th>Total</th></tr></thead><tbody>@foreach($invoice->items as $item)<tr><td>{{ $item->description }}</td><td>{{ $item->quantity }}</td><td>₦{{ number_format($item->unit_price,2) }}</td><td>₦{{ number_format($item->line_total,2) }}</td></tr>@endforeach</tbody></table><h2>Total: ₦{{ number_format($invoice->total,2) }}</h2><h3>Record payment</h3><form method="POST" action="{{ route('invoices.payments.store',$invoice) }}" class="form">@csrf<input name="amount" type="number" step="0.01" placeholder="Amount"><input name="payment_date" type="date" value="{{ date('Y-m-d') }}"><input name="method" value="Bank Transfer"><input name="reference" placeholder="Reference"><button class="btn">Record payment</button></form></div>@endsection
