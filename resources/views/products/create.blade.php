@extends('layouts.app')@section('content')<div class="card"><h1>Create Products</h1><form class="form" method="POST" action="{{ route('products.store') }}">@csrf<label>Name<input name="name" value="{{ old('name',$item->name ?? '') }}"></label><label>Type<input name="type" value="{{ old('type',$item->type ?? '') }}"></label><label>Unit Price<input name="unit_price" value="{{ old('unit_price',$item->unit_price ?? '') }}"></label>@foreach($errors->all() as $error)<div class="error">{{ $error }}</div>@endforeach<button class="btn">Save</button></form></div>@endsection