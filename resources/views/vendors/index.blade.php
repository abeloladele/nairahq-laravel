@extends('layouts.app')@section('content')<div class="actions"><h1>Vendors</h1><a class="btn" href="{{ route('vendors.create') }}">Add new</a></div><table class="table"><tbody>@forelse($items as $item)<tr><td>{{ $item->name ?? $item->description ?? $item->category }}</td><td>{{ $item->email ?? $item->amount ?? $item->unit_price ?? '' }}</td><td><a href="{{ route('vendors.edit',$item) }}">Edit</a></td></tr>@empty<tr><td>No records yet.</td></tr>@endforelse</tbody></table>{{ $items->links() }}@endsection