@extends('layouts.app')@section('content')<div class="card"><h1>New invoice</h1><form method="POST" action="{{ route('invoices.store') }}" class="form">@csrf<label>Customer<select name="customer_id" required>@foreach($customers as $customer)<option value="{{ $customer->id }}">{{ $customer->name }}</option>@endforeach</select></label><label>Issue date<input name="issue_date" type="date" value="{{ date('Y-m-d') }}" required></label><label>Due date<input name="due_date" type="date" value="{{ now()->addDays(14)->format('Y-m-d') }}" required></label><h3>Item</h3><input name="items[0][description]" placeholder="Description" required><input name="items[0][quantity]" type="number" step="0.01" value="1" required><input name="items[0][unit_price]" type="number" step="0.01" placeholder="Unit price" required><input name="items[0][tax_rate]" type="number" step="0.01" value="7.5"><label>Notes<textarea name="notes"></textarea></label>@foreach($errors->all() as $error)<div class="error">{{ $error }}</div>@endforeach<button class="btn">Create invoice</button></form></div>@endsection
