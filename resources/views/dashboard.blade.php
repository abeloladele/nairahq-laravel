@extends('layouts.app')@section('content')<h1>Dashboard</h1><div class="grid cards"><div class="card"><p class="muted">Revenue received</p><div class="kpi">₦{{ number_format($revenue,2) }}</div></div><div class="card"><p class="muted">Outstanding</p><div class="kpi">₦{{ number_format($outstanding,2) }}</div></div><div class="card"><p class="muted">Expenses</p><div class="kpi">₦{{ number_format($expenses,2) }}</div></div><div class="card"><p class="muted">Net profit</p><div class="kpi">₦{{ number_format($revenue-$expenses,2) }}</div></div></div><div class="sidebar"><a class="btn" href="{{ route('invoices.create') }}">New Invoice</a><a class="btn secondary" href="{{ route('expenses.create') }}">Add Expense</a><a class="btn secondary" href="{{ route('customers.create') }}">Add Customer</a></div><div class="grid cards"><div class="card"><h2>Recent invoices</h2>@forelse($invoices as $invoice)<p>{{ $invoice->invoice_number }} - {{ $invoice->customer->name }} <span class="badge">{{ $invoice->status }}</span></p>@empty<p class="muted">No invoices yet.</p>@endforelse</div><div class="card"><h2>Customers</h2>@forelse($customers as $customer)<p>{{ $customer->name }}</p>@empty<p class="muted">No customers yet.</p>@endforelse</div></div>@endsection
