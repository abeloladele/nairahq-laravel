<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>{{ config('app.name','NairaHQ') }}</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body><nav class="nav"><a class="brand" href="{{ route('home') }}">NairaHQ</a><div class="actions">@auth<a href="{{ route('dashboard') }}">Dashboard</a><a href="{{ route('customers.index') }}">Customers</a><a href="{{ route('invoices.index') }}">Invoices</a><a href="{{ route('expenses.index') }}">Expenses</a><form method="POST" action="{{ route('logout') }}">@csrf<button class="btn secondary">Logout</button></form>@else<a href="{{ route('login') }}">Login</a><a class="btn" href="{{ route('register') }}">Start free</a>@endauth</div></nav><main class="container">@if(session('status'))<div class="status">{{ session('status') }}</div>@endif{{ $slot ?? '' }}@yield('content')</main></body></html>
